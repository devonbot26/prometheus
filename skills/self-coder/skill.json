{
    "name": "self-coder",
    "id": "self-coder",
    "version": "1.0.0",
    "description": "Allows Prometheus to write code and create new capabilities for itself.",
    "tools": {
        "create_draft_skill": {
            "path": "index.js",
            "function": "create_draft_skill",
            "description": "Write code for a new skill. Saves it to a staging area for review.",
            "parameters": {
                "name": {
                    "type": "string",
                    "description": "Name of the skill",
                    "required": true
                },
                "description": "What the skill does",
                "code_js": "The JavaScript code for the skill (ESM format)",
                "tool_spec": "The JSON object describing the tool for skill.json"
            }
        },
        "consult_senior_dev": {
            "path": "index.js",
            "function": "consult_senior_dev",
            "description": "Ask the Senior Developer (Gemini Flash) to write complex code or solve hard problems.",
            "parameters": {
                "prompt": {
                    "type": "string",
                    "description": "Detailed description of the code or solution needed",
                    "required": true
                }
            }
        },
        "install_skill": {
            "path": "index.js",
            "function": "install_skill",
            "description": "Install a drafted skill to the active skills directory.",
            "parameters": {
                "name": {
                    "type": "string",
                    "description": "Name of the skill to install",
                    "required": true
                }
            }
        },
        "switch_model": {
            "path": "index.js",
            "function": "switch_model",
            "description": "Swap the agent's brain between local (Qwen) and cloud (Gemini) models.",
            "parameters": {
                "model": {
                    "type": "string",
                    "description": "Target model: 'local' (Qwen), 'gemini' (Gemini Flash), or 'default' (Auto)",
                    "required": true
                }
            }
        },
        "read_file": {
            "path": "index.js",
            "function": "read_file",
            "description": "Read the content of a file (limit 500 lines). Useful for debugging.",
            "parameters": {
                "file_path": {
                    "type": "string",
                    "description": "Path to the file to read (relative to project root)",
                    "required": true
                }
            }
        },
        "apply_patch": {
            "path": "index.js",
            "function": "apply_patch",
            "description": "Apply a precise search-and-replace patch to a file. safe than sed/echo.",
            "parameters": {
                "file_path": {
                    "type": "string",
                    "description": "Path to the file to modify",
                    "required": true
                },
                "target_content": {
                    "type": "string",
                    "description": "Exact string to find and replace (must match exactly)",
                    "required": true
                },
                "replacement_content": {
                    "type": "string",
                    "description": "New string to replace the target with",
                    "required": true
                }
            }
        }
    }
}