{
    "name": "doc-ingestor",
    "description": "Extract structured markdown from rich documents (PDF, DOCX, XLSX, XLSM, NUMBERS, CSV, TXT).",
    "tools": {
        "convert_to_markdown": {
            "path": "index.js",
            "function": "convert_to_markdown",
            "description": "Convert a local document file to markdown format.",
            "parameters": {
                "absolutePath": {
                    "type": "string",
                    "description": "Absolute path to the document file.",
                    "required": true
                }
            }
        }
    }
}